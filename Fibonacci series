// Fibonacci series

#include <iostream>

using namespace std;

int fib(int a)
{
    int f[a+2],i;
    f[0]=0;
    f[1]=1;
    for(i=2;i<=a;i++)
    f[i]=f[i-1]+f[i-2];
    return f[a];
}

int main()
{
    int a;
    cin>>a;
    cout<<"\nResult is "<<fib(a);

    return 0;
}
